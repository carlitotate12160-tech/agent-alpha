# Python
__pycache__/
*.py[cod]
.venv/
.venv312/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Generated protobuf stubs (regenerate with `make proto`)
*_pb2.py
*_pb2.pyi
*_pb2_grpc.py
*.pb.go
*_grpc.pb.go

# Env / secrets
.env
infra/.env
*.key

# Archive / audit (local only, do not push)
archive/
audit_*.jsonl

# Engagement configs (local only, contain target/scope info)
/engagements/*.yaml

# Runner artifacts and reports (local only)
actions-runner/
report.pdf
targets/

# Cowork-mounted working folder â€” never commit the duplicate tree
/Projects/

# Engagement configs anywhere (contain SOW scope) — keep ONLY *.example.yaml
**/*engagement*.yaml
!**/*engagement*.example.yaml

# Temporary review/scratch files (never commit)
fetch_review*.py
pr_body_*.txt
pr_body_*.md
.coverage

# Operational data — client recon reports, never commit (world-readable repo)
*_recon*.pdf
*_report.pdf
*_full_report.pdf
*_recon_blend.pdf
kanopi_recon.pdf
kompresor_recon.pdf
niagamas_*.pdf
softwareakuntan_recon.pdf

# Chain lab artifacts (local only)
infra/chain_lab*

# Pip install artefact (redirect typo)
=4.0.0
