# Python
__pycache__/
*.py[cod]
.venv/
.venv312/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Generated protobuf stubs (regenerate with `make proto`)
*_pb2.py
*_pb2.pyi
*_pb2_grpc.py
*.pb.go
*_grpc.pb.go

# Env / secrets
.env
infra/.env
*.key

# Archive / audit (local only, do not push)
archive/
audit_*.jsonl

# Engagement configs (local only, contain target/scope info)
/engagements/*.yaml

# Runner artifacts and reports (local only)
actions-runner/
report.pdf
targets/

# Cowork-mounted working folder — never commit the duplicate tree
/Projects/

# Engagement configs anywhere (contain SOW scope)  keep ONLY *.example.yaml
**/*engagement*.yaml
!**/*engagement*.example.yaml

# Temporary review/scratch files (never commit)
fetch_review*.py
pr_body_*.txt
pr_body_*.md
.coverage

# Operational data  client recon reports, never commit (world-readable repo)
*_recon*.pdf
*_report.pdf
*_full_report.pdf
*_recon_blend.pdf
client-target_recon.pdf
client-target_recon.pdf
client-target_*.pdf
client-target_recon.pdf

# Chain lab artifacts (local only)
infra/chain_lab*

# Pip install artefact (redirect typo)
=4.0.0

# Operational scripts (contain hardcoded secrets, server IPs, credentials)
restart_services.sh
run_client-target.sh
run_full_chain.sh
enable_active.sh
dispatch_beta.sh
fix_pg_owner.sh
check_func.sh
check_oracle.sh
test_pg_conn.sh
test_pg_store.sh
gen_token.py
debug_engagement.py

# Deployment scripts (local)
deploy_quantum.ps1
user_patch.patch
pr_body_c2.md
deploy_quantum.sh
# Client-specific bug catalogue and raw engagement logs — local only
# These files contain real target names; never commit them.
docs/BUGS.md
docs/BUGS_AND_GAPS_APPENDIX.md
