# Odoo Hardened Real-World Lab — environment variables
# Copy to .env (gitignored) and set real values on your self-owned box.
# TRUE-NEGATIVE target: the admin password must be UNIQUE (not reused from WP/Laravel).

ODOO_ADMIN_PASSWORD=REPLACE_WITH_LAB_VALUE
