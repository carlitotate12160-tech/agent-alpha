[options]
# Hardened Odoo config — TRUE-NEGATIVE lab.
# list_db = False → db_source must NOT be "enumerated".
# The chain gate only chains enumerated access; a guessed db never counts as proven.
list_db = False
admin_passwd = odoo
db_host = db
db_user = odoo
db_password = odoo
db_port = 5432
proxy_mode = True
