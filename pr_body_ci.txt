## Summary

Tiga peningkatan CI untuk skala produksi: coverage measurement, dependency audit, dan fix `make test` target.

## Changes

- **CI (`ci.yml`)**: pytest sekarang berjalan dengan `--cov=agent_alpha --cov-report=term-missing --cov-fail-under=80` (coverage saat ini: 84%)
- **CI (`ci.yml`)**: tambah step `pip-audit --strict` untuk CVE checking pada installed packages
- **`pyproject.toml`**: tambah `pytest-cov` + `pip-audit` ke dependencies
- **`Makefile`**: fix `make test` untuk include `test-phase2` + `test-phase3` (sebelumnya hanya phase_0/1 — menyesatkan)
- **`Makefile`**: tambah `test-phase2` + `test-phase3` targets, update `.PHONY`

## Verification

- 437 passed, 2 skipped
- Coverage: 84.06% (threshold 80% — PASS)
- ruff check: All checks passed
- ruff format: 141 files already formatted
- mypy: Success, 67 source files

## Coverage highlights (areas below 80%)

| Module | Coverage | Note |
|---|---|---|
| `a2a/a2a_pb2_grpc.py` | 0% | Generated protobuf — excluded from lint, not from cov |
| `storage/rls_guard.py` | 0% | Needs integration test (Postgres) |
| `live_fire/chain_runner.py` | 50% | Live-fire path, manual run only |
| `conductor/monologue_transport.py` | 48% | WebSocket transport, needs integration test |
| `events/store.py` | 57% | Postgres/Redis backends, needs integration test |
