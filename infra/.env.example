# Agent-Alpha infra credentials. COPY to infra/.env (gitignored) and set strong,
# unique values. NEVER commit infra/.env or real secrets.
POSTGRES_USER=agent_alpha
POSTGRES_PASSWORD=CHANGE_ME_strong_unique
POSTGRES_DB=agent_alpha
REDIS_PASSWORD=CHANGE_ME_strong_unique
