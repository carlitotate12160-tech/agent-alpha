# WP Lab — environment variables (gitignored, copy to .env)
# MariaDB
MARIADB_ROOT_PASSWORD=wp-lab-root-pass
MARIADB_DATABASE=wordpress
MARIADB_USER=wpuser
MARIADB_PASSWORD=s3cret-wp-x7q2

# The leaked DB password (== WP admin password — the password-reuse vuln)
LEAKED_DB_PASSWORD=s3cret-wp-x7q2
