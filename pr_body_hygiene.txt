## Summary

Remove the accidentally-committed `Projects/Agent-Alpha/` duplicate tree (Cowork-mounted folder that leaked into git) and fix a stale Celery comment.

## Changes

- **git rm -r --cached Projects/** — removed 57 stray files (7079 lines deleted). This was a full duplicate of the canonical `agent_alpha/` tree — anti-Lyndon #2 (ambiguous live-path) and #6 (duplicate canonical copy).
- **.gitignore** — added `/Projects/` to prevent re-commit.
- **agent_alpha/conductor/main.py** — replaced stale comment "Phase 3 placeholder: gate + status only" with accurate description: the Celery task runs the real Alpha->Omega recon pipeline via `recon_runner.run_recon_for_engagement`.

## Verification

- `git ls-files | grep '^Projects/'` → 0 results
- `git grep "Projects.Agent" -- '*.py'` → no hits (dead tree, nothing imports from it)
- pytest: 433 passed, 2 skipped (unchanged — stray copy was dead)
- ruff check: All checks passed
- ruff format: 139 files already formatted
- mypy: Success, 66 source files

## Note

This does NOT rewrite git history. The repo has been public, so the stray files remain in git history. A separate follow-up should consider: (1) making the repo private, (2) git filter-repo / BFG to purge history, (3) rotating any credentials that touched commits.
